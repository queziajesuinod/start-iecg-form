version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: start-iecg-form
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
